export default function AuditLog() {
  return (
    <div className="page">
      <h1>Audit Log</h1>
      <p className="muted">
        Every Diagnose / Fix / Transact / Buyer Agent action — reasoning, action taken, and
        result — will stream here in plain language (Day 4).
      </p>
    </div>
  )
}
